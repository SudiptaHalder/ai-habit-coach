📘 AI Habit Coach – Project Documentation (Short Version)

An interactive, emotional AI Habit Coach that detects your mood, suggests habits, tracks streaks, and responds with expressive animated avatars.

Built using React, Node.js, MySQL, Groq AI, HuggingFace emotion model, and Tailwind animations — fully free.

🚀 Tech Stack
🎨 Frontend

React + TypeScript (Vite)

TailwindCSS v4

Framer Motion (animations)

Lottie (animated avatar faces)

Recharts (charts for mood & progress)

Zustand (state management)

Axios (API communication)

🧠 AI Layer

Groq API (FREE) – Llama 3 for habits, coaching, chat

HuggingFace Emotion Model (FREE) – mood detection

🖥 Backend

Node.js + Express

CORS + dotenv

MySQL2 (database driver)

🛢 Database

MySQL (PlanetScale free or local)

Tables:

users

mood_history

daily_habits

streaks

🎯 Features

Mood detection from text (joy, sad, angry, tired, neutral)

Personalized habit suggestions

Motivational replies based on mood

Personality modes (fun, strict, chill)

Animated avatar faces using Lottie

Daily habit tracking + completion

Mood history chart

Streak counter

Fully responsive UI

🌐 API Endpoints
Method	Route	Description
POST	/analyze-mood	Detect mood using HF model
POST	/coach	AI chat + habit suggestions
POST	/save-mood	Store mood & message
GET	/habits	Get today’s habits
POST	/complete-habit	Mark habit complete
GET	/streak	Get user streak
POST	/update-streak	Update streak logic
📁 Final Folder Structure (Clean + Minimal)
ai-habit-coach/
│
├── frontend/
│   ├── public/
│   ├── src/
│   │   ├── components/
│   │   ├── pages/
│   │   ├── animations/     # Lottie files & SVG avatars
│   │   ├── store/          # Zustand stores
│   │   ├── api/            # Axios API functions
│   │   ├── utils/
│   │   ├── App.tsx
│   │   ├── main.tsx
│   │   └── index.css
│   ├── index.html
│   └── package.json
│
├── backend/
│   ├── src/
│   │   ├── routes/
│   │   │     ├── aiRoutes.js
│   │   │     ├── habitRoutes.js
│   │   │     └── streakRoutes.js
│   │   ├── controllers/
│   │   ├── services/
│   │   ├── utils/
│   │   ├── config/
│   │   │     ├── db.js
│   │   │     └── env.js
│   │   └── server.js
│   ├── .env
│   └── package.json
│
├── database/
│   └── schema.sql
│
└── README.md

📄 Database Schema (Short)
CREATE TABLE users (
  id INT PRIMARY KEY AUTO_INCREMENT,
  name VARCHAR(50),
  personality VARCHAR(20)
);

CREATE TABLE mood_history (
  id INT PRIMARY KEY AUTO_INCREMENT,
  user_id INT,
  mood VARCHAR(20),
  message TEXT,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE daily_habits (
  id INT PRIMARY KEY AUTO_INCREMENT,
  user_id INT,
  habit TEXT,
  completed BOOLEAN DEFAULT 0,
  created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

CREATE TABLE streaks (
  id INT PRIMARY KEY AUTO_INCREMENT,
  user_id INT,
  current_streak INT DEFAULT 0,
  last_updated DATE
);